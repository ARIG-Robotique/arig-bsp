require conf/machine/raspberrypi4-64.conf

# Raspberry Pi specific #
# --------------------- #

# Disable rainbow after gpu firmware is loaded
DISABLE_SPLASH = "1"

# Core frequency
CORE_FREQ = "500"
GPU_MEM = "128"

ENABLE_I2C = "1"
ENABLE_UART = "1"

RPI_EXTRA_CONFIG = "core_freq_min=500"

# ---------------------------------- #

hostname_pn-base-files = "odin"
